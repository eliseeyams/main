<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

<changeSet id="create-users" author="you">
    <sql>CREATE SCHEMA IF NOT EXISTS core;</sql>
    <createTable tableName="users" schemaName="core">
        <column name="id" type="BIGINT" autoIncrement="true">
            <constraints primaryKey="true" nullable="false"/>
        </column>
        <column name="name" type="VARCHAR(255)">
            <constraints nullable="false"/>
        </column>
    </createTable>
</changeSet>

    <!-- Tabelle users anlegen -->
    <changeSet id="002-create-users" author="you">
        <preConditions onFail="MARK_RAN">
            <not><tableExists schemaName="core" tableName="users"/></not>
        </preConditions>

        <createTable tableName="users" schemaName="core">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Rollback (empfohlen) -->
        <rollback>
            <dropTable schemaName="core" tableName="users"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
